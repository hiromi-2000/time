{
  "name": "p5js-music-video",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "biome lint .",
    "lint:fix": "biome lint --apply .",
    "format": "biome format --write .",
    "check": "biome check .",
    "deploy": "pnpm build && gh-pages -d dist"
  },
  "dependencies": {
    "p5": "^1.7.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.1",
    "@types/p5": "^1.7.6",
    "gh-pages": "^6.1.1",
    "typescript": "^5.0.2",
    "vite": "^5.0.0"
  }
}
